<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
            {{user.name}} on {{}}
        </div>
        <div class="text">
            {{comment.description}}
        </div>
    </section>
    {{/each}}
</div>