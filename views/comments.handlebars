  {{#each comments as |comment|}}
<div class="comment-container">
    <section class="comment">
      <div class="meta">
        {{comment.user.name}}:
      </div>
      <div class="text">
        {{comment.description}}
      </div>
    </section>
</div>
  {{/each}}